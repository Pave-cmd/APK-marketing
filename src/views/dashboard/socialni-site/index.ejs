<div class="container">
    <div class="row mb-4">
        <div class="col-md-12">
            <h1 class="h2 mb-3">Správa sociálních sítí</h1>
            <p class="lead">Spravujte své sociální sítě a propojte je s AI marketingem.</p>
        </div>
    </div>

    <div class="row mb-4">
        <div class="col-md-12">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="card-title mb-0">Připojené sociální sítě</h5>
                </div>
                <div class="card-body">
                    <% if (user.socialNetworks && user.socialNetworks.length > 0) { %>
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Sociální síť</th>
                                        <th>Profil</th>
                                        <th>Stav</th>
                                        <th>Akce</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% user.socialNetworks.forEach(function(network) { %>
                                    <tr>
                                        <td>
                                            <% if (network.platform === 'facebook') { %>
                                                <i class="fab fa-facebook text-primary me-2"></i>Facebook
                                            <% } else if (network.platform === 'instagram') { %>
                                                <i class="fab fa-instagram text-danger me-2"></i>Instagram
                                            <% } else if (network.platform === 'twitter') { %>
                                                <i class="fab fa-twitter text-info me-2"></i>Twitter
                                            <% } else if (network.platform === 'linkedin') { %>
                                                <i class="fab fa-linkedin text-primary me-2"></i>LinkedIn
                                            <% } else if (network.platform === 'pinterest') { %>
                                                <i class="fab fa-pinterest text-danger me-2"></i>Pinterest
                                            <% } else if (network.platform === 'tiktok') { %>
                                                <i class="fab fa-tiktok text-dark me-2"></i>TikTok
                                            <% } else if (network.platform === 'youtube') { %>
                                                <i class="fab fa-youtube text-danger me-2"></i>YouTube
                                            <% } %>
                                        </td>
                                        <td><%= network.accountId %></td>
                                        <td>
                                            <% if (network.isConnected) { %>
                                                <span class="badge bg-success">Aktivní</span>
                                            <% } else { %>
                                                <span class="badge bg-warning">Neaktivní</span>
                                            <% } %>
                                        </td>
                                        <td>
                                            <button class="btn btn-sm btn-outline-primary me-1" data-network-id="<%= network.id %>">
                                                <i class="fas fa-cog"></i>
                                            </button>
                                            <button class="btn btn-sm btn-outline-danger network-remove" data-network-id="<%= network.id %>">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </td>
                                    </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        </div>
                    <% } else { %>
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle me-2"></i>
                            Nemáte připojené žádné sociální sítě. Přidejte první síť pomocí tlačítka níže.
                        </div>
                    <% } %>
                    
                    <div class="mt-3">
                        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addNetworkModal">
                            <i class="fas fa-plus me-2"></i>Přidat sociální síť
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="row">
        <div class="col-md-6 mb-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-primary text-white">
                    <h5 class="card-title mb-0">Automatické publikování</h5>
                </div>
                <div class="card-body">
                    <p>Nastavte automatické publikování obsahu na vašich sociálních sítích:</p>
                    <div class="form-check form-switch mb-3">
                        <input class="form-check-input" type="checkbox" id="autoPublish">
                        <label class="form-check-label" for="autoPublish">Povolit automatické publikování</label>
                    </div>
                    
                    <div class="mb-3">
                        <label for="publishFrequency" class="form-label">Frekvence publikování</label>
                        <select class="form-select" id="publishFrequency">
                            <option value="daily">Denně</option>
                            <option value="weekly">Týdně</option>
                            <option value="biweekly">Dvakrát týdně</option>
                            <option value="monthly">Měsíčně</option>
                        </select>
                    </div>
                    
                    <div class="mb-3">
                        <label for="contentType" class="form-label">Typ obsahu</label>
                        <select class="form-select" id="contentType">
                            <option value="blog">Blogové příspěvky</option>
                            <option value="news">Novinky</option>
                            <option value="products">Produkty</option>
                            <option value="mix">Kombinovaný obsah</option>
                        </select>
                    </div>
                    
                    <button class="btn btn-primary">
                        <i class="fas fa-save me-2"></i>Uložit nastavení
                    </button>
                </div>
            </div>
        </div>
        
        <div class="col-md-6 mb-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-primary text-white">
                    <h5 class="card-title mb-0">Statistiky sociálních sítí</h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-primary">
                        <i class="fas fa-chart-line me-2"></i>
                        Statistiky budou dostupné po připojení sociálních sítí a publikování prvních příspěvků.
                    </div>
                    <div class="text-center py-4">
                        <i class="fas fa-chart-bar fa-4x text-muted mb-3"></i>
                        <p>Sledujte dosah a engagement vašich příspěvků na sociálních sítích.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal pro přidání sociální sítě -->
<div class="modal fade" id="addNetworkModal" tabindex="-1" aria-labelledby="addNetworkModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title" id="addNetworkModalLabel">Přidat sociální síť</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form id="addNetworkForm">
                    <div class="mb-3">
                        <label for="networkType" class="form-label">Vyberte sociální síť</label>
                        <select class="form-select" id="networkType" required>
                            <option value="" selected disabled>Vyberte síť</option>
                            <option value="facebook">Facebook</option>
                            <option value="instagram">Instagram</option>
                            <option value="twitter">Twitter</option>
                            <option value="linkedin">LinkedIn</option>
                        </select>
                    </div>
                    
                    <div class="mb-3">
                        <label for="profileName" class="form-label">Název profilu/stránky</label>
                        <input type="text" class="form-control" id="profileName" required>
                    </div>
                    
                    <div class="mb-3">
                        <label for="profileUrl" class="form-label">URL profilu</label>
                        <input type="url" class="form-control" id="profileUrl" required>
                    </div>
                    
                    <div class="alert alert-info">
                        <small>
                            <i class="fas fa-info-circle me-1"></i>
                            Pro propojení sociální sítě budete přesměrováni na přihlašovací stránku vybrané platformy.
                        </small>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Zrušit</button>
                <button type="button" class="btn btn-primary" id="addNetworkBtn">Připojit síť</button>
            </div>
        </div>
    </div>
</div>

<script src="/js/socialni-site.js"></script>